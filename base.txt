
#módulo api:
 É A API que eu uso para receber os arquivos, armazeno eles na pasta uploads.

-a função allowed_file() serve para evitarmos que o usuário mande arquivos errados
-a função upload_file() é para o usuário fazer a requisição e upar o arquivo

#módulo view:
 É a parte de interface para o usuário ver

#módulo perse:
 É aqui que mexo com a API do Llama Parse v1 e faço as requisições necessárias

-Primeiro defino as variáveis de ambiente, note que o arquivo que quero realizar Parsing tenho que colocar manualmente, 
pretendo melhorar isso.
-Depois realizo o post para subir o meu arquivo pdf.
-Por fim utilizo esse metódo polling para fazer as requisições GET que eu quero. Ele serve para dar tempo de o GET chegar na gente,
ou seja, foi utilizado para resolver o problema do assincronismo da API do Llama. É possível se o GET foi bem sucedido no terminal, porque deixei um print(status)
-Os metadados e o texto são todos upados na pasta outputs.